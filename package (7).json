{
  "name": "studybuddy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "StudyBuddy - an AI-powered study platform for school and college students.",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n server,client -c blue,magenta \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "vercel-build": "npm --prefix client run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
